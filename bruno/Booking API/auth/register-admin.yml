info:
  name: register-admin
  type: http
  seq: 3
http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Admin User",
        "email": "admin@test.com",
        "password": "123456"
      }
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
