info:
  name: get-admin-bookings
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/bookings"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
