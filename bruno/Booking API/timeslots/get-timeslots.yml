info:
  name: get-timeslots
  type: http
  seq: 2
http:
  method: GET
  url: "{{baseUrl}}/timeslots"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
